/*
The root is the company itself
every node in the tree represents a car distributor
The leaf nodes are car dealerships
every node holds an integer that is the cost of shipping a car to it
*/
function getCheapestCost(rootNode) {
  salesPathMin = 0;

  return salesPathMin;
}

function Node(cost) {
  this.cost = cost;
  this.children = [];
}
